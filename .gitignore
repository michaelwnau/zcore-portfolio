.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Swap the comments on the following lines if you wish to use zero-installs
# In that case, don't forget to run `yarn config set enableGlobalCache false`!
# Documentation here: https://yarnpkg.com/features/caching#zero-installs

#!.yarn/cache
.pnp.*
node_modules

*storybook.log

.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.test
.env.production

# Ignore these build artifacts
build
dist

# Ignore coverage directory generated by tools like istanbul, playwright, chromatic, etc.
coverage
playwright-report

# Ignore the output of the coverage tool
coverage/lcov.info
coverage/lcov-report
coverage/coverage-final.json
coverage/coverage-summary.json

# Ignore any local env files
*.local

# log files
*.log
yarn-error.log

# Ignore IDE files
.idea

